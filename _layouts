---
layout: default
---

<h1>{{ page.title }}</h1>
<p>{{ page.description }}</p>

{% if page.image %}
<img src="{{ page.image }}" alt="Project image">
{% endif %}

<a href="{{ page.demo_link }}">View Demo</a> | <a href="{{ page.repo_link }}">View Repository</a>
